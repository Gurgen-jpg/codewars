function wave(str){
    result = [];
    for (let i=0; i< str.length; i++){
        if (str[i] !== ' ') {
            let temp = str.slice(0,i) + str.charAt(i).toUpperCase() + str.slice(i+1);
            result.push(temp);
        } else {
            continue
        }
    }
    return result; 
}
