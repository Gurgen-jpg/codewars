function countPositivesSumNegatives(input) {
    if (input == null || input.length == 0 ) {
        return []
    } else {
    let b = input.filter(el => el < 0).reduce(function(sum, el){ return sum+=el},0)
    let a = input.filter(el => el > 0).length;        
      return [a,b]}
  }
